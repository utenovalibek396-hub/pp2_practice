import math
print(math.sqrt(64))
print(math.ceil(1.1))
print(math.floor(1.9))
print(math.pi)
print(math.pow(3,4))
# sqrt() means square root.
# ceil() rounds the number up to the nearest integer.
# floor() rounds the number down to the nearest integer.
# pi is a mathematical constant (π).
# pi is a mathematical constant (π).
